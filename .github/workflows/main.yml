name: VPS Emulator Eternal V4 (Buffered Sync)

on:
  workflow_dispatch:
    inputs:
      generation:
        default: '1'

jobs:
  vps-runtime:
    runs-on: ubuntu-latest
    env:
      IDRIVE_ACCESS_KEY: "1wSsXXLgOy5R200QdufF"
      IDRIVE_SECRET_KEY: "VLmiPgwEwYGijMrry8jGXV2R3Cw2uSr5inFJoZnR"
      IDRIVE_ENDPOINT: "https://j7a4.la.idrivee2-35.com"
      BUCKET_NAME: "storage"
      PM2_HOME: /home/runner/.pm2
    
    steps:
      - name: Checkout Bootloader
        uses: actions/checkout@v4

      - name: Provision & Pull
        run: |
          sudo npm install -g pm2
          chmod +x install.sh
          ./install.sh
          echo "/usr/local/bin" >> $GITHUB_PATH

      - name: Restore PM2 State (Surgical)
        run: |
          # Start daemon
          pm2 ping
          
          if [ -f $PM2_HOME/dump.pm2 ]; then
            echo "🔄 Repairing paths..."
            node -e "
              const fs = require('fs');
              const path = process.env.PM2_HOME + '/dump.pm2';
              if (fs.existsSync(path)) {
                let data = fs.readFileSync(path, 'utf8');
                data = data.replace(/\/home\/runner\/work\/[^/]+\/[^/]+/g, process.env.GITHUB_WORKSPACE);
                fs.writeFileSync(path, data);
              }
            "
            pm2 resurrect || echo "New setup."
          fi

      - name: Background Sync Loop
        run: |
          (
            while true; do
              sleep 180 # Sync every 3 mins
              pm2 save --force > /dev/null 2>&1
              rclone sync /home/runner/home idrive:$BUCKET_NAME
              echo "☁️ Snapshot synced to iDrive at $(date)"
            done
          ) &

      - name: Connectivity Info
        run: |
          tmate -S /tmp/tmate.sock new-session -d
          tmate -S /tmp/tmate.sock wait tmate-ready
          echo "---------------------------------------------------"
          echo "SSH: $(tmate -S /tmp/tmate.sock display -p '#{tmate_ssh}')"
          echo "WEB: $(tmate -S /tmp/tmate.sock display -p '#{tmate_web}')"
          echo "---------------------------------------------------"

      - name: 5.5-Hour Flush & Handoff
        env:
          GH_TOKEN: ${{ secrets.MY_PAT }}
        run: |
          sleep 19800
          echo "🚨 Finalizing Generation..."
          
          # Final persistence push
          pm2 save --force
          rclone sync /home/runner/home idrive:$BUCKET_NAME
          
          NEXT_GEN=$(( ${{ github.event.inputs.generation }} + 1 ))
          curl -L -X POST \
            -H "Authorization: token $GH_TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/${{ github.repository }}/actions/workflows/vps-eternal-v4.yaml/dispatches \
            -d "{\"ref\":\"${{ github.ref_name }}\", \"inputs\": {\"generation\": \"$NEXT_GEN\"}}"
          sleep 600
          
